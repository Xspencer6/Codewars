def validate_pin(pin):
    #check if there's alphapet
    numbers = '1234567890'
    for char in pin:
        if char in numbers:
            continue
        else:
            return False
    if len(pin) == 4 or len(pin) == 6:
        return True
    else: 
        return False
    
